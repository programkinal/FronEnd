import { AssignmentInstructorCourse } from './assignment-instructor-course';

describe('AssignmentInstructorCourse', () => {
  it('should create an instance', () => {
    expect(new AssignmentInstructorCourse()).toBeTruthy();
  });
});
